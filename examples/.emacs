;;; .emacs --- WY's emacs config
;;; Commentary:
;;; configuration based on Purcell's work

;;; Code:

(load-file "~/.emacs.d/init.el")
(load-file "~/.wy/emacs.d/wy-vanilla.el")

(provide '.emacs)
;;; .emacs ends here
